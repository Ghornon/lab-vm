http {
    server {
        listen 80;
        server_name localhost;
        
        location / {
            add_header X-Hostname $hostname;
            return 200 "This request was served by $hostname";
        }
    }
}
